import React from 'react';
import { OctavesRange, selectKey } from '../../domain/keyboard';
import { note } from '../../domain/note';

function Keyboard(): JSX.Element {
  return <div>keyboard component</div>;
}

export default Keyboard;
